import { RouterConfig } from '@angular/router';
import {BindingsDemoComponent} from "./bindings-demo.component";

export const BindingsDemoRoutes: RouterConfig = [
    {
        path: 'bindings-demo',
        component: BindingsDemoComponent
    }
];
